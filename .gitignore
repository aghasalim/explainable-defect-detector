.venv/
__pycache__/
*.pyc
.pytest_cache/

# 150 MB+ per category; fetch_mvtec.py rebuilds it from the tracked index
data/mvtec/

# anomaly maps are large float arrays, regenerated by any run
reports/*.npy
.claude/
.env
.env.local

# editable install artefact, rebuilt by uv sync
*.egg-info/

# build products of the cross-language checks in verify/
verify/*/target/
verify/*.class
