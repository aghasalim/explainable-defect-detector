# Runtime deps for the hosted demo (Streamlit Community Cloud / Docker / HF Spaces).
#
# The +cpu local version is pinned deliberately. On linux-amd64 the default PyPI
# `torch` is the CUDA build (~800 MB of nvidia-* wheels), which exhausts the
# free-tier resource limit for a demo that never touches a GPU.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
torchvision==0.28.0+cpu
streamlit>=1.40
numpy>=2.0
pillow>=10
matplotlib>=3.8
